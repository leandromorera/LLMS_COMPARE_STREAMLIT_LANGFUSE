OPENAI_API_KEY=your_openai_api_key_here

LANGFUSE_PUBLIC_KEY=pk-lf-...

LANGFUSE_SECRET_KEY=sk-lf-...

LANGFUSE_HOST=http://localhost:3000

# Optional: default OpenAI models for the 3 judges

JUDGE_MODEL_1=gpt-4o-mini
JUDGE_MODEL_2=gpt-4.1-mini
JUDGE_MODEL_3=gpt-4o

# Optional: OpenAI request tuning

OPENAI_TIMEOUT_SECONDS=60



# from project root
python -m pytest tests/ -v

# single module
python -m pytest tests/test_vote.py -v

# only fast tests (no OpenAI calls — all are already mocked)
python -m pytest tests/ -v